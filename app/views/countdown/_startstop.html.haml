-if check_running(tk)
  = button_to(starttimer_countdown_index_path(room:tk.room, stoptimer:"stop"), class:'btn btn-small btn-primary') do
    %i.fa.fa-stop
-else
  = button_to(starttimer_countdown_index_path(room:tk.room, stoptimer:"play"), class:'btn btn-small btn-primary') do
    %i.fa.fa-play
